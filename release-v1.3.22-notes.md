# v1.3.22

本版本集中修复 GPT Image 异步任务的结果下载、断点续查和详情图进度显示。

- 兼容 Meta/Clash 等 TUN Fake-IP 网络：仅对 HTTPS 域名、`198.18.0.0/15` Fake-IP 且本机路由源地址匹配的连接启用兼容路径；继续执行固定地址、对端校验、TLS/SNI、禁止重定向、MIME 与图片解码验证。
- 已持久化任务 ID 后，状态查询遇到网络错误、HTTP 408、429 或 5xx 时保留为可恢复任务，下次继续查询同一任务，不会重新提交付费 POST。
- 白底图、场景图及每张详情图的异步进度在原行刷新，避免轮询日志持续堆积。
- 详情图的“生成完成，正在安全下载图片”只更新到对应的 `详情图 N` 行，不再生成位置错乱的通用详情图状态行。
- 保留动态详情图数量、图片比例、最多 14 张参考图、任务检查点和完成商品即时归档逻辑。
