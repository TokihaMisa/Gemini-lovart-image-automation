# v1.3.27

本版本改善 GPT Image 异步任务失败后的补偿重试和轮询日志。

- 服务端明确返回“服务器过载”且任务已终态失败时，按网络/服务异常类别进入用户配置的补偿重试队列；仍在处理中的任务继续查询，不重复提交。
- 提交结果未知、需要人工确认等情况仍禁止自动重投，避免重复生成和扣费。
- 支撑图和详情图恢复轮询时减少重复的阶段日志；已保存的任务 ID、提示词和生成结果继续复用。
- 绘制中的累计等待时间以原始任务创建时间计算，便于辨认真正的长时间任务。
