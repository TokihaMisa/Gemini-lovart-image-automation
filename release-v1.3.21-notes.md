# v1.3.21

本版本将 GPT Image 生图模式迁移到可配置 Base URL 的异步媒体任务协议，并集中修复长任务在本地显示、恢复和付费安全方面的问题。

- 使用 JSON `POST /v1/media/generate` 创建任务，再通过 `GET /v1/media/status` 按任务 ID 轮询结果。
- 支持最多 14 张参考图，并在请求发出前校验单图、总图片和请求体大小；需要时仍可手动启用本地参考拼图合并。
- 创建成功后立即持久化任务 ID、状态与输入身份；程序重启或本地等待超时后继续查询原任务，不会再次提交同一付费任务。
- 白底图、场景图与每张详情图均显示真实阶段、平台进度、等待时长和脱敏任务标识；完成的商品继续立即移动到已完成目录。
- 保留动态详情图数量和原有图片比例/分辨率映射，每个详情提示词仍只生成一张图。
- API 密钥只保存在本地 `.env`，不会写入配置、状态、日志或请求指纹；结果图片下载继续执行 SSRF、TLS、MIME 与图像解码校验。
- 移除旧 HAPI 专用逻辑、同步 OpenAI Images 接口及同步回退。旧的无任务 ID 运行中检查点无法恢复，会在首次迁移时安全失效。
