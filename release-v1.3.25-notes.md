# v1.3.25

本版本修复 Windows 下状态文件偶发“拒绝访问”，并避免多个后台任务同时写入同一输出目录。

- `status.json` 改用同目录唯一临时文件原子更新，避免多个写入方争用固定的 `status.json.tmp`。
- 遇到 Windows 瞬时访问拒绝、共享冲突或锁冲突时自动短暂退避重试，不再立即判定商品失败。
- 同一输出目录增加跨进程运行保护；重复启动会显示正在运行的 PID，防止旧后台任务与新任务互相覆盖。
- 后台进程已结束后，遗留的运行锁会自动回收；正常完成或异常退出时会主动释放。
- WebUI 以 `[UI_PRODUCT]` 结构化数据为准，自动纠正先前普通控制台日志造成的商品名称乱码。
- 保留现有断点续跑、任务检查点和不同输出目录并行运行能力。
