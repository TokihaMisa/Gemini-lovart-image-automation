# v1.3.24

本版本重点解决大批量、长时间运行时 WebUI 内存持续增长并最终出现 `Out of Memory` 的问题。

- 商品原图改为按需生成 96x96 JPEG 缩略图，先缩小再处理 EXIF 方向，避免界面刷新反复携带或复制大图。
- 状态面板固定显示当前任务和最近 50 个任务，并及时释放不可见任务的缩略图数据。
- 每个商品只保留最近 20 条界面日志；任务目录批量载入后统一刷新，避免大批量启动时生成大量中间页面。
- WebUI 日志通道改为有界队列；页面关闭或断开后停止读取，避免无人消费的日志无限堆积。
- 无论打包版还是源码版，界面输出管道断开时后台任务都可继续执行，不再因 Windows `Invalid argument` 崩溃。
- 状态统计增加已跳过任务，并正确显示仍在平台后台执行的任务。
- 保留现有任务检查点与重启续查逻辑，不会因为这次界面内存优化而重新提交已创建的付费任务。
