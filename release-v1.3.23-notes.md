# v1.3.23

本版本新增问鼎与 Sub2API 生图服务切换，并重点改进 Sub2API 长任务的恢复、并行调度和付费安全。

- 设置中可在问鼎 API 与 Sub2API 之间切换，分别保存密钥、Base URL、模型、分辨率和参考图合并选项。
- Sub2API 使用可恢复异步任务：创建后立即保存任务 ID，程序重启或查询暂时失败时继续查询同一任务，不重复提交付费请求。
- 默认允许 2 个商品的平台生图任务并行，可在设置中调整为 1–8；本地处理仍保持顺序执行，避免同时写入同一商品目录。
- 运行中的平台任务默认每 10 秒自动复查一次；到达并行上限后优先续查旧任务，再提交新任务，避免长任务被后续商品饿死。
- 修正 Sub2API 的 1K、2K、4K 尺寸映射和计费档位，避免界面选择 1K 时被代理按 2K 计费。
- 增加浏览器型 Cloudflare 代理兼容，同时保留 API 密钥本地保存、任务检查点、结果图片安全下载和最多 14 张参考图逻辑。
