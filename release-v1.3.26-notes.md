# v1.3.26

本版本修复 GPT Image 异步任务过期后反复轮询同一批商品、长期无法产出图片的问题，并完善缺图恢复策略。

- 服务端已删除的旧任务不再被无限视为“仍在绘制中”，超过 10 分钟后会进入明确的过期任务恢复流程。
- 默认保留商品目录中已经完成且有效的图片，只重新提交缺失的支撑图或详情页，减少重复生成和额外扣费。
- 新提交任务遇到短暂 404 时保留 10 分钟同步宽限，避免服务端尚未同步完成就重复提交。
- 补图完成后按磁盘上的实际有效图片数量验收；数量仍不一致时自动整套重做一次。
- 整套重做使用输入与提示词指纹记录，同一批内容最多执行一次，避免无限重做。
- 本地一次恢复仍失败时停止全局重复提交，并保留失败状态供用户检查或下次手动续跑。
